# macOS
.DS_Store
**/.DS_Store

# Editor / Tools
.claude/

# Original-Fotos (Quellmaterial, NICHT fürs Web – die optimierten liegen in assets/)
# Nur der Root-Ordner "Fotos" – mit führendem / verankert, damit assets/fotos/ NICHT betroffen ist
/Fotos/

# Dependencies / Logs (falls je verwendet)
node_modules/
*.log
